{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "out",
  "build": {
    "env": {
      "NEXT_TELEMETRY_DISABLED": "1"
    }
  }
}
